# Sleep Airline Phase 階段設計用意與架構分析

## 摘要

本文檔詳細說明 Sleep Airline 睡眠旅程系統中 Phase（階段）機制的設計用意、架構理念與用戶體驗考量。系統採用 8 個 Phase（Phase 0-7）來模擬完整的飛行旅程體驗，將睡眠過程轉化為沉浸式的航空旅行隱喻。

---

## 一、設計理念與核心思想

### 1.1 隱喻設計（Metaphor Design）

系統採用「睡眠即飛行」的隱喻設計，將睡眠過程類比為航空旅行：

- **睡眠準備** → **登機準備**
- **入睡過程** → **起飛與巡航**
- **醒來過程** → **降落與抵達**
- **目的地選擇** → **旅行目的地選擇**

這種隱喻設計的優勢：
1. **降低認知負擔**：用戶對航空旅行流程熟悉，無需學習新概念
2. **增強沉浸感**：通過視覺、聽覺、觸覺多感官體驗強化隱喻
3. **情感連結**：旅行隱喻喚起探索與期待的情感

### 1.2 狀態機模式（State Machine Pattern）

系統採用狀態機模式管理 Phase 轉換，確保：
- **單向流動**：Phase 只能前進，不能倒退（通過 `maxReachedPhase` 機制）
- **狀態隔離**：每個 Phase 有獨立的 UI、邏輯和資源管理
- **可預測性**：明確的轉換條件和觸發機制

---

## 二、Phase 階段詳細設計

### Phase 0: POST_ARRIVAL（醒來後的窗戶存在狀態）

**設計用意：**
- **目的**：展示上次旅程的結果，建立連續性體驗
- **功能**：
  - 顯示上次降落的城市和國家（從 Firebase 載入）
  - 顯示上次旅程的降落圖片（800x800）
  - 提供「開始新旅程」的入口
- **UI 狀態**：顯示 `stateCountry`（所在國家資訊）
- **用戶操作**：單擊開始新旅程，雙擊快速重啟，三擊查看歷史

**設計考量：**
- 使用漸層背景或上次圖片，避免空白感
- 從 Firebase 載入最新記錄，確保狀態持久化
- 隱藏 `stateLandingCountryInfo`，避免重複顯示

---

### Phase 1: TIME_SELECTION（選擇睡眠時長）

**設計用意：**
- **目的**：讓用戶設定本次睡眠的預期時長
- **功能**：
  - 提供 6 個時長選項（1.5小時、3小時、4.5小時、6小時、7.5小時、9小時）
  - 符合 90 分鐘睡眠週期理論
  - 使用旋鈕選擇，提供觸覺反饋
- **UI 狀態**：顯示 `stateTimer`（時間選擇進度條）
- **用戶操作**：旋轉旋鈕選擇時長，按下按鈕確認

**設計考量：**
- 時長選項基於睡眠科學（90 分鐘週期）
- 視覺化進度條，直觀展示選擇
- 自動選擇第一個選項，減少操作步驟

---

### Phase 2: DIRECTION_SELECTION（選擇旅程氛圍／氣候帶）

**設計用意：**
- **目的**：讓用戶選擇旅程的「氛圍」或「氣候帶」，影響目的地選擇
- **功能**：
  - 提供 6 個氣候帶選項（熱帶夢境、溫帶探索、寒帶靜謐等）
  - 每個氣候帶對應不同的緯度範圍和城市類型
  - 影響目的地計算算法
- **UI 狀態**：顯示 `stateAngle`（氣候帶選擇）
- **用戶操作**：旋轉旋鈕選擇氣候帶，按下按鈕確認

**設計考量：**
- 使用「氛圍」而非「目的地」，保持神秘感
- 氣候帶對應真實地理緯度，確保目的地合理性
- 提供描述性文字，幫助用戶理解選擇

---

### Phase 3: DESTINATION_SHOW（顯示目的地）

**設計用意：**
- **目的**：展示計算出的目的地，建立期待感
- **功能**：
  - 顯示目的地城市、國家、國旗
  - 顯示氣候帶描述
  - 生成並準備起飛語音（TTS）
- **UI 狀態**：顯示 `stateDestination`（目的地資訊）
- **用戶操作**：按下按鈕確認並開始起飛

**設計考量：**
- 提前生成 TTS，減少等待時間
- 顯示完整目的地資訊（城市 + 國家），增強真實感
- 使用國旗 emoji，視覺化國家資訊

---

### Phase 4: TAKEOFF_PREP（起飛準備）

**設計用意：**
- **目的**：模擬登機和起飛準備過程，建立儀式感
- **功能**：
  - 播放機長廣播（captain.mp3 + TTS）
  - 顯示「機長廣播中...」動態 UI
  - 播放起飛影片（takeoff1.mp4 → takeoff2.mp4）
  - 提示用戶拉下窗簾（實體操作）
- **UI 狀態**：顯示 `stateReady_to_fly`（準備起航資訊）
- **用戶操作**：按下按鈕觸發起飛序列

**設計考量：**
- 機長廣播建立權威感和儀式感
- 動態 UI 提供即時反饋，避免用戶等待焦慮
- 起飛影片視覺化「起飛」過程
- 窗簾操作增加實體互動，強化沉浸感

---

### Phase 5: CRUISE（巡航階段）

**設計用意：**
- **目的**：主要的睡眠階段，提供背景視覺和音效
- **功能**：
  - 播放循環背景影片（takeoff2.mp4）
  - 顯示飛行進度條（從出發地到目的地）
  - 支援旋鈕觸發飛機餐（一次旅程只觸發一次）
  - 支援按鈕操作（單擊查看時間，雙擊觸發飛機餐，提前降落）
- **UI 狀態**：顯示 `flightProgressWrapper`（飛行進度條）
- **用戶操作**：
  - 旋鈕轉動：觸發飛機餐
  - 單擊：查看當前時間
  - 雙擊：觸發飛機餐（備用）
  - 長按：提前降落

**設計考量：**
- 背景影片不干擾睡眠，低優先級播放
- 進度條提供時間感知，但不顯示具體時間（避免焦慮）
- 飛機餐增加互動性，但限制為一次（避免過度干擾）
- 暫停 takeoff2.mp4 以提升按鈕檢測穩定性

---

### Phase 6: DESCENT（降落前五分鐘）

**設計用意：**
- **目的**：提前預告降落，讓用戶有心理準備
- **功能**：
  - 播放降落預告廣播（TTS）
  - 可選：播放 wakeup 音樂（提前降落時）
  - 顯示「準備降落」提示
- **UI 狀態**：保持進度條顯示
- **用戶操作**：按下按鈕可手動觸發降落

**設計考量：**
- 提前 5 分鐘預告，符合真實航班體驗
- 可選的 wakeup 音樂，提供溫和喚醒
- 手動降落選項，增加用戶控制感

---

### Phase 7: LANDING（降落完成）

**設計用意：**
- **目的**：完成旅程，展示目的地，收集回饋
- **功能**：
  - 播放機長降落廣播（captain.mp3 + TTS）
  - 顯示「機長廣播中...」動態 UI
  - 播放降落影片（landing.mp4）
  - 顯示目的地圖片（800x800，AI 生成）
  - 收集情緒量表回饋（1-5 分）
  - 顯示目的地資訊（城市 + 國家）
- **UI 狀態**：
  - 隱藏進度條
  - 顯示目的地圖片
  - 顯示情緒量表（`stateFlightFeedback`）
  - 最後顯示 `stateCountry`（所在國家）
- **用戶操作**：
  - 旋鈕選擇情緒分數（1-5）
  - 按下按鈕確認回饋
  - 再次按下返回 Phase 0

**設計考量：**
- 確保 takeoff2.mp4 持續播放直到 landing.mp4 準備就緒，避免白屏
- 情緒量表收集用戶體驗數據，用於優化系統
- 目的地圖片永久儲存，避免過期問題
- 完整的降落流程，建立「抵達」的儀式感

---

## 三、狀態管理設計

### 3.1 Phase 與 State 的雙層架構

系統採用 **Phase（階段）** 和 **State（狀態）** 的雙層架構：

- **Phase**：宏觀的旅程階段（0-7），定義整體流程
- **State**：微觀的 UI 狀態（COUNTRY, TIMER, ANGLE, DESTINATION, READY_TO_FLY），定義具體顯示內容

**設計優勢：**
1. **關注點分離**：Phase 管理流程，State 管理 UI
2. **靈活性**：同一 Phase 可以有不同的 State 組合
3. **可維護性**：修改 UI 不影響流程邏輯

### 3.2 不可逆機制（One-Way Flow）

通過 `maxReachedPhase` 變數確保 Phase 只能前進：

```javascript
let maxReachedPhase = Phase.POST_ARRIVAL; // 記錄已到達的最高 phase
```

**設計用意：**
- 模擬真實旅程：一旦起飛，不能回到登機階段
- 防止狀態混亂：避免用戶操作導致意外回退
- 確保數據一致性：每個 Phase 的數據只在該階段有效

### 3.3 狀態重置機制

`resetFlightState()` 函數確保返回 Phase 0 時完全重置：

**重置內容：**
- 布林標記（landingExecuted, midnightServiceActive 等）
- 按鈕狀態（buttonPressSequence, lastButtonPressTime）
- 定時器（sleepTimer, flightProgressInterval）
- 影片狀態（takeoff2 可見性檢查）
- 音頻狀態（earlyLandingWakeupMusic）
- 準備好的語音（preparedAnnouncement）

**設計用意：**
- 確保每次旅程都是全新開始
- 防止舊狀態影響新旅程
- 釋放資源，避免記憶體洩漏

---

## 四、用戶體驗設計考量

### 4.1 漸進式披露（Progressive Disclosure）

系統採用漸進式披露原則，每個 Phase 只顯示必要資訊：

- **Phase 0-2**：選擇階段，只顯示選擇相關資訊
- **Phase 3**：目的地展示，建立期待
- **Phase 4**：起飛準備，建立儀式感
- **Phase 5**：巡航階段，最小化干擾
- **Phase 6-7**：降落階段，完整體驗

### 4.2 即時反饋機制

每個 Phase 轉換都提供即時視覺反饋：

- **Phase 3→4**：立即顯示「機長廣播中...」UI
- **Phase 6→7**：立即顯示「機長廣播中...」UI
- **按鈕操作**：立即更新 UI，不等待後端響應

**設計用意：**
- 減少用戶等待焦慮
- 提供操作確認
- 增強系統響應感

### 4.3 錯誤處理與降級策略

系統包含多層錯誤處理：

1. **圖片載入失敗**：使用預設圖片或漸層背景
2. **目的地計算失敗**：觸發蟲洞效應（Wormhole），使用備用目的地
3. **API 調用失敗**：使用本地生成內容
4. **影片播放失敗**：靜默處理，不影響主流程

**設計用意：**
- 確保系統穩定性
- 提供優雅的降級體驗
- 避免單點故障影響整體

---

## 五、技術架構設計

### 5.1 異步操作管理

每個 Phase 轉換涉及多個異步操作：

- **圖片生成**：調用 OpenAI API（可能需要數十秒）
- **TTS 生成**：調用 TTS 服務
- **影片播放**：等待影片載入和播放完成
- **Firebase 同步**：保存記錄到資料庫

**設計策略：**
- 使用 `Promise` 和 `async/await` 管理異步流程
- 提前生成內容（如 Phase 3 提前生成 TTS）
- 並行執行不相關的操作
- 使用 `waitForCompletion` 選項確保順序

### 5.2 資源管理

系統需要管理多種資源：

- **影片資源**：takeoff1.mp4, takeoff2.mp4, landing.mp4
- **音頻資源**：captain.mp3, TTS 音頻
- **圖片資源**：目的地圖片（AI 生成）
- **定時器**：sleepTimer, flightProgressInterval

**設計策略：**
- 明確的生命週期管理（創建、使用、清理）
- 防止資源洩漏（清除 interval、移除事件監聽器）
- 優先級管理（背景影片低優先級，按鈕檢測高優先級）

### 5.3 性能優化

系統針對性能進行多項優化：

1. **影片優化**：
   - takeoff2.mp4 使用極簡化檢查邏輯（每 30 秒檢查一次）
   - 優先保證按鈕響應，背景影片可容忍失敗

2. **按鈕檢測優化**：
   - Phase 5 暫停 takeoff2.mp4，提升按鈕檢測穩定性
   - 使用 `buttonPressSequence` 陣列追蹤多擊事件

3. **UI 更新優化**：
   - 強制更新 UI，確保與旋鈕位置同步
   - 降低輪詢間隔（100ms），提升響應速度

---

## 六、設計模式應用

### 6.1 觀察者模式（Observer Pattern）

系統使用事件驅動架構：

- `switchToPhase()` 觸發 Phase 轉換
- `switchState()` 更新 UI 狀態
- 按鈕事件觸發 Phase 轉換

### 6.2 策略模式（Strategy Pattern）

不同 Phase 使用不同的處理策略：

- `handleTakeoffPrep()`：處理起飛準備
- `handleCruise()`：處理巡航階段
- `handleDescent()`：處理降落預告
- `handleLanding()`：處理降落完成

### 6.3 單例模式（Singleton Pattern）

全局狀態管理：

- `currentPhase`：當前階段
- `destinationData`：目的地數據
- `selectedTimer`：選擇的時長

---

## 七、設計原則總結

### 7.1 用戶中心設計（User-Centered Design）

- **降低學習成本**：使用熟悉的航空旅行隱喻
- **提供控制感**：每個階段都有明確的操作選項
- **即時反饋**：每個操作都有視覺或聽覺反饋

### 7.2 穩定性優先（Stability First）

- **錯誤處理**：多層錯誤處理和降級策略
- **狀態管理**：明確的狀態轉換和重置機制
- **資源管理**：完整的生命週期管理

### 7.3 性能優化（Performance Optimization）

- **異步操作**：並行執行不相關操作
- **資源優先級**：關鍵功能優先於背景任務
- **響應速度**：降低輪詢間隔，提升 UI 響應

---

## 八、未來擴展性

### 8.1 可擴展的 Phase 架構

系統設計允許未來添加新的 Phase：

- **Phase 8+**：可以添加「中轉站」、「特殊活動」等階段
- **插件機制**：每個 Phase 可以獨立開發和部署

### 8.2 可配置的 Phase 流程

未來可以實現：

- **自定義 Phase 順序**：根據用戶偏好調整流程
- **跳過某些 Phase**：快速模式，跳過選擇階段
- **Phase 分支**：根據條件進入不同的 Phase 分支

---

## 結論

Sleep Airline 的 Phase 階段設計是一個完整的用戶體驗系統，通過：

1. **隱喻設計**：將睡眠轉化為航空旅行體驗
2. **狀態機模式**：確保流程的可預測性和穩定性
3. **漸進式披露**：逐步引導用戶完成旅程
4. **錯誤處理**：提供優雅的降級體驗
5. **性能優化**：確保系統響應速度

這種設計不僅提供了沉浸式的用戶體驗，還確保了系統的穩定性和可維護性，為未來的功能擴展奠定了堅實的基礎。

---

**版本**：1.0  
**日期**：2025-12-25  
**作者**：Sleep Airline Design Team

