# TTS 語音生成時機總覽

## 📋 各階段 TTS 生成時機

### 🛫 **Phase 3 → Phase 4: 起飛階段**

#### **時機 1: 目的地選擇完成後（Phase 3）**
- **函數**: `prepareForTakeoff()`
- **位置**: `pi.html:3948-3977`
- **生成內容**: 起飛廣播 TTS 文字
- **生成方式**: 
  - 調用 `generateTakeoffAnnouncement()` 生成文字
  - 背景調用 `generateTtsAudio()` 預先生成音頻（不阻塞）
- **狀態**: 保存到 `preparedAnnouncement` 和 `preparedAnnouncementGender`
- **時機**: 用戶選擇目的地後，進入「準備起航」狀態時

#### **時機 2: 按鈕確認後（Phase 3 → Phase 4 轉換）**
- **位置**: `pi.html:3881-3900`
- **行為**: **檢查並使用已生成的 TTS**（不重複生成）
  - 如果 `preparedAnnouncement` 已存在（時機1已生成），直接使用
  - 如果 `preparedAnnouncement` 不存在（時機1未生成），才生成
- **時機**: 用戶按下按鈕確認起飛時
- **播放**: Phase 4 播放 `captain.mp3` 後播放已準備好的 TTS

#### **播放時機: Phase 4 起飛階段**
- **位置**: `pi.html:4079-4092`
- **播放順序**:
  1. 播放 `captain.mp3`（前 6 秒）
  2. 播放預先生成的 TTS 音頻（如果有的話）
  3. 如果沒有預先生成，則即時生成並播放

---

### 🛬 **Phase 5: 提前降落（緊急降落）**

#### **時機: 確認提前降落後**
- **函數**: `confirmEarlyLanding()`
- **位置**: `pi.html:8915-9070`
- **流程**:
  1. **步驟 3**: 開始準備 TTS 文字內容（`landingContentPromise`，異步進行）
     - 調用 `generateLandingContent()` 生成降落廣播文字
     - 計算最終目的地
     - 生成降落內容（包含時間差、目的地等信息）
   
  2. **步驟 4**: 等待 TTS 文字內容準備完成（確保文字內容已生成）
     - `await landingContentPromise`
   
  3. **步驟 5**: TTS 文字內容準備完成後，立即預先生成 TTS 音頻（**關鍵優化**）
     - 調用 TTS API (`/tts/play`) 生成音頻
     - 等待音頻生成完成（15 秒超時）
     - 標記 `ttsReady = true`
   
  4. **步驟 6**: TTS 音頻準備好後，播放 `captain.mp3`
     - 顯示「機長廣播中」
     - 播放 `captain.mp3`
   
  5. **步驟 7**: `captain.mp3` 播放完成後，立即播放 TTS
     - 調用 `playAnnouncement()` 播放已預先生成的 TTS
     - 應該可以立即播放（無延遲）

**關鍵點**: 
- **先等待文字內容完成，再生成音頻**（確保播放正確內容）
- TTS 音頻在播放 `captain.mp3` **之前**就已經生成完成
- 邏輯更簡單、更安全，避免播放錯誤內容

---

### 🛬 **Phase 6: 準時/延後降落**

#### **時機: 按下按鈕後（Phase 6 → Phase 7）**
- **函數**: `triggerManualLanding()`
- **位置**: `pi.html:10900-10950`
- **流程**:
  1. **步驟 1**: **先預先生成 TTS 音頻**（**關鍵優化：爭取時間**）
     - 使用臨時文字模板立即開始生成音頻
     - 調用 TTS API (`/tts/play`) 生成音頻（不等待文字內容）
     - 與文字內容生成**並行進行**，爭取時間
     - 等待音頻生成完成（15 秒超時）
   
  2. **步驟 2**: 準備 TTS 文字內容（與音頻生成並行）
     - 調用 `generateDescentWakeUpAnnouncement()` 生成準備降落的語音文字
     - 這是同步函數，立即返回文字模板
   
  3. **步驟 3**: 如果最終文字內容與臨時模板不同，使用最終內容重新生成音頻
     - 如果文字已準備好且不同，重新生成 TTS 音頻
     - 確保使用最終文字內容的音頻
   
  4. **步驟 4**: 等待音頻生成完成（確保音頻準備好）
   
  5. **步驟 5**: TTS 音頻準備好後，播放 `captain.mp3`
     - 顯示「機長廣播中」
     - 播放 `captain.mp3`
   
  6. **步驟 6**: `captain.mp3` 播放完成後，立即播放 TTS
     - 調用 `playAnnouncement()` 播放已預先生成的 TTS
     - 應該可以立即播放（無延遲）

**關鍵點**: 
- **先生成音頻，再生成文字**（爭取時間，因為要先播放音頻）
- TTS 音頻在播放 `captain.mp3` **之前**就已經生成完成
- 使用臨時模板先生成音頻，文字準備好後如有差異則重新生成

---

## 🎯 **優化重點**

### ✅ **已優化的階段**:
1. **Phase 5 (提前降落)**: ✅ TTS 音頻預先生成
2. **Phase 6 (準時/延後降落)**: ✅ TTS 音頻預先生成

### ⚠️ **潛在改進點**:
1. **Phase 4 (起飛階段)**: 
   - 目前是背景生成，可能不夠及時
   - 建議：在播放 `captain.mp3` 之前確保 TTS 音頻已生成完成

---

## 📊 **TTS 生成時機對比表**

| 階段 | TTS 文字生成 | TTS 音頻生成 | 播放時機 | 優化狀態 |
|------|------------|------------|---------|---------|
| **Phase 3 → 4 (起飛)** | Phase 3 選擇目的地後 | 背景生成（不阻塞） | Phase 4 播放 captain.mp3 後 | ⚠️ 可優化 |
| **Phase 5 (提前降落)** | 確認降落後（步驟 3） | 播放 captain.mp3 前（步驟 5） | captain.mp3 播放後（步驟 7） | ✅ 已優化 |
| **Phase 6 (準時/延後)** | 按下按鈕後（步驟 1） | 播放 captain.mp3 前（步驟 2） | captain.mp3 播放後（步驟 4） | ✅ 已優化 |

---

## 🔍 **代碼位置參考**

### Phase 3 → 4 (起飛)
- 文字生成: `pi.html:3948-3977` (`prepareForTakeoff`)
- 按鈕確認生成: `pi.html:3881-3896`
- 播放: `pi.html:4079-4092`

### Phase 5 (提前降落)
- 文字生成: `pi.html:8930-8977` (`landingContentPromise`)
- 音頻生成: `pi.html:8983-9024` (步驟 5)
- 播放: `pi.html:9043-9050` (步驟 7)

### Phase 6 (準時/延後降落)
- 文字生成: `pi.html:10850-10852` (步驟 1)
- 音頻生成: `pi.html:10854-10890` (步驟 2)
- 播放: `pi.html:10903-10911` (步驟 4)

