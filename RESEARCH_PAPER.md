# Sleep Airline: 沉浸式飛行模擬睡眠體驗系統

## 📋 項目概述

**Sleep Airline** 是一個基於 Web 技術的沉浸式飛行模擬睡眠體驗系統，旨在通過模擬飛行旅程的方式，為用戶提供一個舒適、有趣的睡眠與甦醒體驗。系統結合了實體硬體交互、AI 圖像生成、文字轉語音（TTS）、目的地計算算法等多種技術，創造出一個完整的虛擬飛行體驗。

### 核心概念

系統將睡眠過程類比為一次飛行旅程：
- **起飛**：用戶開始睡眠
- **巡航**：深度睡眠階段
- **降落**：甦醒過程
- **抵達**：回到現實世界，到達新的「目的地」

---

## 🏗️ 系統架構

### 技術棧

| 技術類別 | 使用技術 |
|---------|---------|
| **前端框架** | 原生 HTML5 + CSS3 + JavaScript (ES6+) |
| **後端 API** | Python Flask (推測) |
| **數據庫** | Firebase Realtime Database |
| **AI 圖像生成** | DALL-E 3 API (透過自定義 API) |
| **語音合成** | TTS API (支援多語言、多性別) |
| **硬體交互** | RESTful API (旋鈕位置、按鈕狀態) |
| **媒體播放** | HTML5 Audio/Video API |

### 系統模組

```
┌─────────────────────────────────────────────────┐
│              Sleep Airline System                │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  Phase 管理  │  │  狀態管理     │          │
│  │  系統        │  │  系統         │          │
│  └──────────────┘  └──────────────┘          │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  目的地計算  │  │  AI 圖像生成  │          │
│  │  算法        │  │  系統         │          │
│  └──────────────┘  └──────────────┘          │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  TTS 語音    │  │  音頻管理    │          │
│  │  合成系統    │  │  系統         │          │
│  └──────────────┘  └──────────────┘          │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  硬體交互    │  │  Firebase    │          │
│  │  API         │  │  數據存儲    │          │
│  └──────────────┘  └──────────────┘          │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🎯 核心功能

### 1. 多階段飛行流程（Phase System）

系統採用階段式設計，將整個飛行體驗分為 8 個階段：

| Phase | 名稱 | 描述 | 主要功能 |
|-------|------|------|---------|
| **Phase 0** | POST_ARRIVAL | 抵達後 | 顯示目的地資訊、選擇下次飛行參數 |
| **Phase 1** | TIME_SELECTION | 時間選擇 | 用戶選擇睡眠時長（1-5 小時） |
| **Phase 2** | DIRECTION_SELECTION | 方向選擇 | 用戶選擇飛行方向（東/西/隨機） |
| **Phase 3** | DESTINATION_SHOW | 目的地展示 | 顯示起點和終點城市 |
| **Phase 4** | TAKEOFF | 起飛 | 播放起飛影片和機長廣播 |
| **Phase 5** | CRUISE | 巡航 | 顯示飛行進度條、提供飛機餐服務 |
| **Phase 6** | DESCENT | 降落前 | 播放喚醒音樂、準備降落 |
| **Phase 7** | LANDING | 降落 | 播放降落影片、顯示新目的地、情緒反饋 |

#### Phase 系統特點

- **不可逆性**：系統記錄最高到達的 Phase，確保流程不可逆
- **狀態管理**：每個 Phase 都有獨立的 UI 狀態和邏輯
- **平滑過渡**：Phase 之間使用淡入淡出效果，提供流暢的視覺體驗

### 2. 智能目的地計算系統

系統根據用戶的睡眠時長和選擇的方向，智能計算「降落」的城市。

#### 計算邏輯

```javascript
// 核心計算參數
- 睡眠時長（分鐘）
- 飛行方向（東/西/隨機）
- 氣候帶匹配
- 緯度範圍限制
- 距離計算（基於時差）
```

#### 算法特點

1. **多模式計算**：
   - 短距離模式（< 30 分鐘）
   - 中距離模式（30-120 分鐘）
   - 長距離模式（> 120 分鐘）

2. **氣候帶匹配**：
   - 根據睡眠時長匹配對應的氣候帶
   - 確保目的地與飛行時長相關

3. **蟲洞效應**：
   - 1/7 機率觸發特殊效果
   - 將用戶傳送到隨機的遠距離城市

4. **城市分層系統**：
   - 根據距離自動選擇合適的城市對
   - 確保每次飛行都有新鮮感

### 3. AI 圖像生成系統

系統使用 DALL-E 3 API 生成降落目的地的視覺圖像。

#### 圖像生成流程

1. **參考圖像**：使用 `taipei.png` 作為風格參考
2. **提示詞優化**：
   - 強調第一人稱視角
   - 明確排除窗框、玻璃反射等元素
   - 確保全畫幅、無遮擋的視野

3. **生成策略**：
   - 預生成：在 Phase 7 開始時就開始生成圖片
   - 並行處理：圖片生成與其他流程並行進行
   - 緩存機制：使用 Promise 確保只生成一次

#### 技術實現

```javascript
// 圖像生成 API 調用
async function generateLandingImage(destination, referenceImage) {
    const prompt = `第一人稱視角，從飛機窗戶向外看，
                    目的地：${destination.city}, ${destination.country}，
                    無窗框、無玻璃反射，全畫幅視野...`;
    
    const response = await fetch('/api/generate-image', {
        method: 'POST',
        body: JSON.stringify({
            prompt: prompt,
            referenceImage: referenceImage // base64 編碼
        })
    });
    
    return await response.json();
}
```

### 4. 文字轉語音（TTS）系統

系統整合了多語言、多性別的 TTS 功能，為不同階段提供語音廣播。

#### TTS 功能特點

1. **預生成機制**：
   - 使用 `/tts/generate` API 預先生成音頻
   - 使用 `/tts/play_cached` API 播放已生成的音頻
   - 確保語音在需要時立即可用

2. **多語言支援**：
   - 中文（繁體）：`zh-TW`
   - 可擴展至其他語言

3. **語音參數**：
   - 性別選擇（male/female）
   - 語速調整（0.75-0.9）
   - 音調調整（0.8-0.9）

4. **應用場景**：
   - 起飛廣播（Phase 4）
   - 降落廣播（Phase 7）
   - 目的地介紹（Phase 7）
   - 喚醒語音（Phase 6）

#### TTS 生成時機

| 階段 | 生成時機 | 播放時機 |
|------|---------|---------|
| **Phase 4** | 起飛準備時 | 起飛影片播放後 |
| **Phase 6** | 10 秒後自動生成 | 自動播放（或按鈕觸發） |
| **Phase 7** | 降落準備時 | captain.mp3 播放後立即播放 |

### 5. 音頻管理系統

系統實現了複雜的音頻播放和音量控制邏輯。

#### 音頻類型

1. **背景音樂**：
   - 起飛音樂（takeoff）
   - 巡航音樂（cruise）
   - 飛機餐音樂（meal）
   - 喚醒音樂（wakeup，多首隨機）

2. **音效**：
   - 機長廣播音效（captain.mp3，6 秒）
   - 按鈕點擊音效

3. **語音**：
   - TTS 生成的語音廣播

#### 音量控制策略

- **淡入淡出**：所有音樂使用淡入淡出效果，避免突兀
- **動態調整**：語音播放時，背景音樂自動降低到 0.15
- **恢復機制**：語音播放完成後，音樂音量恢復到 0.6

#### 技術實現

```javascript
// 音頻淡入函數
async function fadeInAudio(audio, duration, targetVolume) {
    const startVolume = audio.volume;
    const startTime = Date.now();
    
    return new Promise((resolve) => {
        const interval = setInterval(() => {
            const elapsed = Date.now() - startTime;
            const progress = Math.min(elapsed / duration, 1);
            audio.volume = startVolume + (targetVolume - startVolume) * progress;
            
            if (progress >= 1) {
                clearInterval(interval);
                resolve();
            }
        }, 16); // 約 60fps
    });
}
```

### 6. 硬體交互系統

系統通過 RESTful API 與實體硬體（旋鈕、按鈕）進行交互。

#### 硬體組件

1. **旋鈕（Knob）**：
   - 用於選擇睡眠時長（1-5 小時）
   - 用於選擇飛行方向（東/西/隨機）
   - 用於情緒反饋評分（1-5 分）

2. **按鈕（Button）**：
   - 確認選擇
   - 觸發降落
   - 確認提前降落（三下）

#### API 端點

```javascript
// 讀取旋鈕位置
GET /api/knob/position
Response: { position: 0-4 }

// 讀取按鈕狀態
GET /api/button/state
Response: { pressed: true/false }
```

#### 輪詢機制

系統使用輪詢機制持續監聽硬體狀態：

```javascript
// 旋鈕位置輪詢（每 100ms）
const knobPollInterval = setInterval(async () => {
    const position = await fetchKnobPosition();
    updateSelection(position);
}, 100);
```

### 7. 情緒反饋系統

系統在降落後收集用戶的情緒反饋，用於改善體驗。

#### 反饋機制

1. **視覺設計**：
   - 5 顆星星的評分系統
   - 根據旋鈕位置動態顯示亮起的星星數量
   - 類似於睡眠時長選擇的交互方式

2. **評分範圍**：
   - 1-5 分（對應旋鈕位置 0-4）

3. **超時機制**：
   - 30 秒無操作自動使用當前選擇

4. **數據存儲**：
   - 反饋分數保存到 Firebase
   - 用於後續分析和改善

### 8. Firebase 數據存儲

系統使用 Firebase 存儲飛行記錄和用戶數據。

#### 存儲數據結構

```javascript
{
    userDisplayName: "Pi User",
    dataIdentifier: "sleep_record_" + timestamp,
    groupName: "Pi",
    eventType: "SLEEP_RECORD",
    timestamp: Date.now(),
    sleepDuration: 90, // 分鐘
    direction: "WESTBOUND",
    origin: {
        city: "Taipei",
        country: "Taiwan",
        lat: 25.0330,
        lon: 121.5654
    },
    destination: {
        city: "Tokyo",
        country: "Japan",
        lat: 35.6762,
        lon: 139.6503
    },
    destinationInfo: {
        time: "當地時間 14:30",
        weather: "晴朗，25°C",
        feature: "探索東京的傳統與現代..."
    },
    destinationImageUrl: "https://...",
    feedbackScore: 4,
    isWormhole: false
}
```

---

## 🔄 用戶流程

### 完整飛行流程

```
┌─────────────────────────────────────────────────┐
│  Phase 0: 抵達後                                │
│  - 顯示上次降落的目的地                          │
│  - 顯示目的地資訊（時間、天氣、特色）            │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 1: 時間選擇                              │
│  - 旋鈕選擇睡眠時長（1-5 小時）                  │
│  - 按鈕確認                                      │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 2: 方向選擇                              │
│  - 旋鈕選擇飛行方向（東/西/隨機）                │
│  - 按鈕確認                                      │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 3: 目的地展示                            │
│  - 顯示起點和終點城市                            │
│  - 顯示飛行資訊                                  │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 4: 起飛                                  │
│  - 播放起飛影片                                  │
│  - 機長廣播（TTS）                               │
│  - 背景音樂淡入                                  │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 5: 巡航                                  │
│  - 顯示飛行進度條                                │
│  - 可選：飛機餐服務（中斷睡眠）                  │
│  - 可選：提前降落（按三下確認）                  │
└─────────────────┬───────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐   ┌──────────────────┐
│ 提前降落     │   │ 正常流程         │
│ Phase 5 → 7  │   │ Phase 5 → 6      │
└──────────────┘   └──────────────────┘
        │                   │
        └─────────┬─────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 6: 降落前（僅正常流程）                   │
│  - 播放喚醒音樂（0.15 音量淡入）                  │
│  - 10 秒後自動播放喚醒語音                       │
│  - 音樂音量恢復到 0.6                            │
│  - 等待用戶按鈕進入 Phase 7                       │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  Phase 7: 降落                                  │
│  - 播放新的 wakeup 音樂                          │
│  - 計算最終目的地                                │
│  - 生成降落圖片（背景）                          │
│  - 播放機長廣播（僅提前降落）                    │
│  - 播放降落影片（landing.mp4）                   │
│  - 顯示情緒量表                                  │
│  - 顯示新目的地資訊                              │
│  - 播放目的地介紹語音                            │
│  - 保存到 Firebase                               │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│  返回 Phase 0                                   │
│  - 顯示新目的地                                  │
│  - 顯示新目的地資訊                              │
│  - 重置所有狀態                                  │
│  - 準備下次飛行                                  │
└─────────────────────────────────────────────────┘
```

---

## 🎨 UI/UX 設計

### 設計原則

1. **沉浸式體驗**：
   - 800x800 的「窗戶」視窗模擬飛機窗戶
   - 全屏顯示，減少干擾
   - 平滑的過渡動畫

2. **一致性**：
   - 統一的白色背景資訊欄
   - 一致的按鈕和交互元素
   - 統一的字體和顏色方案

3. **反饋機制**：
   - 即時的視覺反饋
   - 清晰的狀態指示
   - 平滑的動畫過渡

### 主要 UI 組件

1. **窗戶視窗**（`.window-content`）：
   - 800x800 像素
   - 顯示飛行影片、目的地圖片
   - 支持背景圖片和影片播放

2. **資訊欄**（`.info-bar`）：
   - 底部固定位置
   - 顯示當前階段資訊
   - 支持多種狀態（計算中、機長廣播、情緒量表等）

3. **進度條**（`.flight-progress-bar`）：
   - Phase 5 顯示
   - 動態更新飛行進度
   - 顯示剩餘時間

4. **情緒量表**（`#stateFlightFeedback`）：
   - 5 顆星星評分系統
   - 根據旋鈕位置動態顯示
   - 30 秒超時機制

---

## 🔬 技術亮點

### 1. 異步流程管理

系統大量使用 `async/await` 和 `Promise` 來管理複雜的異步流程：

```javascript
// 並行處理多個異步任務
const [landingData, destinationInfo, imagePromise] = await Promise.all([
    generateLandingContent(...),
    generateDestinationInfo(...),
    generateLandingImage(...)
]);
```

### 2. 狀態管理

使用全局變數和標記來管理複雜的狀態：

```javascript
// 防止重複執行
if (landingExecuted) return;
landingExecuted = true;

// 保存 Promise 引用
window._phase7ImageAPromise = generateLandingImage(...);
window._landingFeedbackPromise = showFlightFeedback(...);
```

### 3. 定時器管理

系統實現了完整的定時器清理機制，防止內存洩漏：

```javascript
// 保存定時器引用
window._phase6AutoPlayTimeout = setTimeout(...);
window._phase6VolumeFadeInterval = setInterval(...);

// 清理定時器
if (window._phase6AutoPlayTimeout) {
    clearTimeout(window._phase6AutoPlayTimeout);
    window._phase6AutoPlayTimeout = null;
}
```

### 4. 錯誤處理

系統實現了完善的錯誤處理機制：

```javascript
try {
    await playAudioFile('sounds/captain.mp3', 6);
} catch (error) {
    console.warn('⚠️ captain.mp3 播放失敗:', error);
    // 繼續執行，不中斷流程
}
```

### 5. 性能優化

- **預生成機制**：TTS 音頻和圖片在需要前就開始生成
- **並行處理**：多個任務同時進行，不阻塞主流程
- **緩存機制**：使用 Promise 確保資源只生成一次

---

## 📊 研究價值

### 1. 睡眠科學應用

- **睡眠時長可視化**：將抽象的睡眠時長轉化為具體的飛行距離
- **甦醒過程優化**：通過漸進式喚醒（音樂、語音、視覺）改善甦醒體驗
- **情緒追蹤**：收集用戶情緒反饋，用於睡眠質量分析

### 2. 人機交互研究

- **實體交互設計**：旋鈕和按鈕的物理交互體驗
- **多模態反饋**：視覺、聽覺、觸覺的綜合反饋
- **沉浸式體驗**：通過模擬環境創造沉浸感

### 3. AI 技術整合

- **AI 圖像生成**：使用 DALL-E 3 生成個性化目的地圖像
- **自然語言處理**：生成目的地介紹和廣播內容
- **智能計算**：根據用戶選擇智能計算目的地

### 4. 數據驅動改善

- **用戶行為追蹤**：記錄每次飛行的詳細數據
- **情緒數據收集**：收集用戶情緒反饋
- **持續優化**：基於數據持續改善系統

---

## 🚀 未來擴展

### 1. 功能擴展

- **多語言支援**：擴展至更多語言
- **更多目的地**：增加更多城市和特色地點
- **個性化定制**：根據用戶偏好定制體驗
- **社交功能**：分享飛行記錄、比較飛行距離

### 2. 技術優化

- **離線模式**：支持離線使用
- **性能優化**：減少加載時間，提升流暢度
- **跨平台**：支持移動設備和桌面設備

### 3. 研究應用

- **睡眠研究**：與睡眠研究機構合作，收集睡眠數據
- **治療應用**：用於睡眠障礙的輔助治療
- **教育應用**：用於地理和時間概念的教學

---

## 📝 結論

**Sleep Airline** 是一個創新的沉浸式睡眠體驗系統，通過模擬飛行旅程的方式，為用戶提供了一個有趣、舒適的睡眠與甦醒體驗。系統整合了多種先進技術，包括 AI 圖像生成、TTS 語音合成、智能目的地計算等，創造出一個完整的虛擬飛行體驗。

系統的設計充分考慮了用戶體驗，通過平滑的過渡、即時的反饋、沉浸式的視覺效果，創造出一個令人愉悅的交互體驗。同時，系統的模組化設計和良好的錯誤處理機制，確保了系統的穩定性和可擴展性。

這個系統不僅是一個有趣的應用，更是一個具有研究價值的平台，可以用於睡眠科學、人機交互、AI 技術等多個領域的研究和應用。

---

## 📚 參考資料

### 技術文檔

- `LANDING_LOGIC_SUMMARY.md` - 降落邏輯完整說明
- `PHASE7_FLOW.md` - Phase 7 完整流程
- `TTS_GENERATION_TIMELINE.md` - TTS 生成時機說明

### 相關技術

- [DALL-E 3 API Documentation](https://platform.openai.com/docs/guides/images)
- [Firebase Realtime Database](https://firebase.google.com/docs/database)
- [Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)
- [HTML5 Audio/Video API](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement)

---

## 👥 作者資訊

**項目名稱**：Sleep Airline  
**開發時間**：2024  
**技術棧**：HTML5, CSS3, JavaScript, Python, Firebase, DALL-E 3  
**許可證**：待定

---

*本文檔用於學術研究和技術探討，如有疑問或建議，歡迎聯繫。*

